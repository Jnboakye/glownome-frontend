import tokens from './tokens';

/**
 * Font families, for Paper props that take a style object rather than a class
 * (labelStyle, titleStyle, textStyle). Everything else uses font-* classes.
 */
export const fonts = tokens.fonts;
